# Folder ini akan berisi model ML (.h5 / .tflite / .keras)
# File model tidak di-commit ke Git karena ukurannya besar
# Gunakan .gitignore atau Git LFS untuk model besar

# Contoh model yang akan disimpan di sini:
# - category_classifier.h5       (Model klasifikasi kategori)
# - spending_predictor.keras     (Model prediksi pengeluaran)
